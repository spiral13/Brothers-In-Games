/**
 * Initial State---------------------------------------------------------
 */
const initialState = {

};

/**
 * Types----------------------------------------------------------------
 */

/**
 * Reducer-------------------------------------------------------------
 */
const reducer = (state = initialState, action = {}) => {
  switch (action.type) {

    default:
      return state;
  }
};


/**
 * Action Creators------------------------------------------------------
 */

/**
  * Display after loading page
  */

/**
 * Export
 */
export default reducer;
